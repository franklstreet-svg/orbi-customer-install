flask>=3.0,<4
edge-tts>=7.0
pywebpush>=2.0
py-vapid>=1.9
cryptography>=42
pypdf>=4.0
python-docx>=1.1
openpyxl>=3.1
google-auth>=2.16
google-auth-oauthlib>=1.0
google-api-python-client>=2.80
# Document + image generation. Required at orbi.py import time
# because modules/invoice_pdf.py, image_gen.py, etc. import these at
# module load. Missing any of them = ImportError = orbi service never
# starts on a fresh customer install (smoke test caught this).
reportlab>=4.0
Pillow>=10.0
python-pptx>=0.6.21
matplotlib>=3.8
requests>=2.31
# Phone / payment integrations
stripe>=8.0
twilio>=9.0
# Thermal printer dispatch (web_driver/print_dispatch.py)
python-escpos>=3.0
# Misc utilities
qrcode>=7.0
# Web agent (browser control) — drives a real Chromium for the scraper
# AND the web_agent module that lets Orbi operate any web app the
# customer already uses (dispatch systems, supplier portals, etc.).
# Chromium itself is downloaded by install_runtime.py via
# `playwright install chromium --with-deps` post-pip.
playwright>=1.45
# Native desktop window for the dashboard. Wraps Windows' built-in
# WebView2 (already on every Windows 10/11) so the customer sees a real
# program window with the Orbi icon — not Chrome --app mode. ~2 MB.
pywebview>=5.1; sys_platform == "win32"
